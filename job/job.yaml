apiVersion: batch/v1
kind: Job
metadata:
  name: failed-job
spec:
  backoffLimit: 2
  activeDeadlineSeconds: 20
  template:
    spec:
      containers:
      - name: busybox
        image: busybox
        command: 
          - /bin/sh 
          - -c 
          - "exit 1"
      restartPolicy: Never